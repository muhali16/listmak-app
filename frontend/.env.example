# ListMak App Environment Configuration (Example)

# API Configuration
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Dev server (vite.config.js only)
VITE_SERVER_HOST=localhost
VITE_SERVER_PORT=5173
VITE_SERVER_ALLOWED_HOSTS=localhost
VITE_API_PROXY_TARGET=http://localhost:9001

# Note: testing mode is now a runtime setting toggled by admin (backend /config),
# no longer a build-time env var.
